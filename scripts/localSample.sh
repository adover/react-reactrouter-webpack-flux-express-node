# rename to server/local.sh and set variable to match your local environment
# OPTIONAL: the port to use when running npm run server, defaults to 3001
export PORT=3003;
export DEV_PORT=8081;
export NGROK_KEY=mq;

# currently not in use
# export LOCALHOST_API=http://sylviapark.kpr.dev/wp-json/;
